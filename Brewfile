# Tokyo Hiker repository dependencies managed with Homebrew.
# Usage:
#   brew bundle --file=./Brewfile
#   pnpm setup-brew

# Node.js and package manager setup (scripts/setup-node.sh)
brew "nodenv"
brew "node-build"
brew "pnpm"

# AI-assisted writing and review
brew "claude-code"
